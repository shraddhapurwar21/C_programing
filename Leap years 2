#include <stdio.h>
int main()
{
    int n, i;
    scanf("%d", &n);
    for (i = 1; i <= n; i++)
    {
        if (i % 400 == 0)
        {
            printf("%d ", i);
        }
        else if (i % 4 == 0 && i % 100 != 0)
        {
            printf("%d ", i);
        }
    }
    return 0;
}
